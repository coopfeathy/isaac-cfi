---
phase: 06-lead-nurturing-career-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/careers/page.tsx
  - app/page.tsx
  - app/pricing/page.tsx
  - app/discovery-flight/page.tsx
autonomous: false
requirements:
  - LEAD-03
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04

must_haves:
  truths:
    - "/careers page renders 5 career stages: Student -> PPL -> Instrument Rating -> CFI -> Hired at Merlin"
    - "/careers page has scroll-driven active stage tracking with IntersectionObserver"
    - "/careers page sticky sidebar shows current stage name, title, and progress bar"
    - "Homepage contains 'train here, get hired here' callout with link to /careers near instructor section"
    - "Pricing page contains career callout with link to /careers near CFI cert tier"
    - "Discovery flight page contains career callout with link to /careers near CTA"
    - "Blog JSON-LD Article schema still present (no regression from PUB-03)"
    - "SocialMediaFeed component still renders on homepage (no regression from PUB-04)"
    - "Admin ProspectsTab lead_stage dropdown still functions (LEAD-03 verification)"
  artifacts:
    - path: "app/careers/page.tsx"
      provides: "Career pipeline page with 5 stages"
      contains: "Fly Here. Get Hired Here."
      min_lines: 100
    - path: "app/page.tsx"
      provides: "Homepage with career callout added"
      contains: "/careers"
    - path: "app/pricing/page.tsx"
      provides: "Pricing page with career callout added"
      contains: "/careers"
    - path: "app/discovery-flight/page.tsx"
      provides: "Discovery flight page with career callout added"
      contains: "/careers"
  key_links:
    - from: "app/careers/page.tsx"
      to: "/private-pilot-timeline"
      via: "Secondary CTA link"
      pattern: "See Training Timeline"
    - from: "app/page.tsx"
      to: "/careers"
      via: "Inline callout link"
      pattern: "Learn about the career path"
    - from: "app/pricing/page.tsx"
      to: "/careers"
      via: "Inline callout link"
      pattern: "See how"
    - from: "app/discovery-flight/page.tsx"
      to: "/careers"
      via: "Inline callout link"
      pattern: "Learn more"
---

<objective>
Create the /careers page with a visual 5-stage career pathway, add "train here, get hired here" callouts to the homepage, pricing, and discovery flight pages, verify LEAD-03 is met by existing ProspectsTab, and confirm no regression on blog SEO and social feed.

Purpose: Makes the "train here, get hired here" career narrative visible across the site, converting more discovery flight prospects into long-term students who see Merlin as a career launchpad.
Output: New /careers page, three pages with career callouts, regression verification for blog/social/prospect pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lead-nurturing-career-content/06-CONTEXT.md
@.planning/phases/06-lead-nurturing-career-content/06-RESEARCH.md
@.planning/phases/06-lead-nurturing-career-content/06-UI-SPEC.md

@app/private-pilot-timeline/page.tsx
@app/page.tsx
@app/pricing/page.tsx
@app/discovery-flight/page.tsx
@app/admin/tabs/ProspectsTab.tsx
@app/blog/[slug]/page.tsx
@app/components/SocialMediaFeed.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /careers page mirroring private-pilot-timeline pattern</name>
  <files>app/careers/page.tsx</files>
  <read_first>
    app/private-pilot-timeline/page.tsx,
    .planning/phases/06-lead-nurturing-career-content/06-UI-SPEC.md,
    .planning/phases/06-lead-nurturing-career-content/06-CONTEXT.md
  </read_first>
  <action>
    Create `app/careers/page.tsx` following the exact structural pattern of `app/private-pilot-timeline/page.tsx`. This is a `'use client'` page with IntersectionObserver scroll tracking.

    1. **Metadata export** (above the client component or in a separate layout — match how private-pilot-timeline handles it):
       ```typescript
       // If private-pilot-timeline exports metadata at file level, do the same.
       // Otherwise, create app/careers/layout.tsx with:
       export const metadata: Metadata = {
         title: 'Pilot Career Path | Merlin Flight Training',
         description: 'Train at Merlin and build a career in aviation. Student to CFI to hired instructor — the full pathway explained.',
         openGraph: {
           title: 'Pilot Career Path | Merlin Flight Training',
           description: 'Train at Merlin and build a career in aviation.',
           url: 'https://merlinflighttraining.com/careers',
           type: 'website',
         },
       }
       ```
       NOTE: If `private-pilot-timeline/page.tsx` uses `'use client'` and cannot export metadata, check if there's a `layout.tsx` in that directory. Mirror the same approach.

    2. **Career stage data array** — 5 stages, inline in the component. Use the exact data from UI-SPEC Career Stage Data Contract:

       ```typescript
       const careerStages = [
         {
           phase: 'Stage 1',
           title: 'Start Your Journey',
           duration: '0 hours · No certificate required',
           goals: [
             'Take your discovery flight',
             'Enroll and meet your instructor',
             'Set your training schedule',
           ],
           milestone: 'You are officially a student pilot.',
         },
         {
           phase: 'Stage 2',
           title: 'Private Pilot Certificate',
           duration: '~60–80 hours total',
           goals: [
             'Build foundational aircraft control',
             'Solo flights and cross-country navigation',
             'Pass the FAA written test and checkride',
           ],
           milestone: 'Private Pilot Certificate earned.',
         },
         {
           phase: 'Stage 3',
           title: 'Instrument Rating',
           duration: '~50 additional hours',
           goals: [
             'Train to fly in clouds and low visibility',
             'Master approach procedures and IFR navigation',
             'Pass the instrument written test and checkride',
           ],
           milestone: 'Instrument Rating added to certificate.',
         },
         {
           phase: 'Stage 4',
           title: 'CFI Certificate',
           duration: '~20–40 additional hours',
           goals: [
             'Learn to teach and evaluate students',
             'Complete the FOI and FIA knowledge tests',
             'Pass the CFI checkride with an FAA examiner',
           ],
           milestone: 'Certified Flight Instructor certificate earned.',
         },
         {
           phase: 'Stage 5',
           title: 'Career at Merlin',
           duration: 'Hired · Building flight hours',
           goals: [
             'Join the Merlin instructor team',
             'Build hours toward commercial minimums',
             'Merlin graduates may be considered for affiliated aviation opportunities as the organization grows.',
           ],
           milestone: 'You fly. We pay. Career in aviation begins.',
         },
       ]
       ```

    3. **Page structure** per UI-SPEC Layout Contract:
       - Dark hero section: full-width with dark gradient overlay (`bg-gradient-to-br from-black via-gray-900 to-black`), h1 "Fly Here. Get Hired Here." (display text: `text-3xl sm:text-4xl md:text-5xl font-bold text-white`), gold divider bar (`w-16 h-1 bg-golden`), sub-headline "From your first discovery flight to a paid flying career — Merlin is the full path." (`text-base text-gray-300`)
       - Main content: `max-w-6xl mx-auto px-4 sm:px-6 py-10 sm:py-14`, grid `grid-cols-1 lg:grid-cols-[260px_1fr] gap-8 lg:gap-12`
       - Left sticky sidebar (`lg:sticky lg:top-28`): "Current Stage" label (12px/700 uppercase gray-500), stage name (24px/700 darkText), stage title (12px/400 gray-600), "Typical Timeline" label, timeline value, progress bar (`h-2 w-full bg-gray-200 rounded-full` with inner `bg-golden` fill at `width: ((activeIndex+1)/stages.length)*100%` with `transition-all duration-300`)
       - Right timeline list (`space-y-12` with `relative pl-10`): for each stage, connector dot (absolute `left-0`, `h-6 w-6 rounded-full border-2`; active: `border-golden bg-black`, inactive: `border-gray-300 bg-white`), card (`rounded-2xl border border-gray-200 bg-white p-6 shadow-sm`), stage tag (`text-xs font-bold uppercase text-golden`), title h2 (`text-2xl font-bold text-darkText`), duration meta (`text-xs text-gray-500`), goals list with gold dots (`text-base text-gray-700`), milestone callout (`bg-golden/10 border border-golden/30 rounded-lg px-4 py-3 text-base font-bold text-darkText`)

    4. **IntersectionObserver** — match `private-pilot-timeline/page.tsx` exactly:
       - `useRef` array for each stage card
       - `useState` for `activeIndex` (default 0)
       - `IntersectionObserver` with `threshold: 0.45`, updates `activeIndex` on intersect
       - Cleanup observer on unmount

    5. **Bottom CTAs** — flex-wrap gap-3:
       - Primary: `<Link href="/discovery-flight">` — "Book Your Discovery Flight" — `bg-black rounded-lg px-6 py-3 font-bold text-white hover:bg-gray-900`
       - Secondary: `<Link href="/private-pilot-timeline">` — "See Training Timeline" — `border border-golden rounded-lg px-6 py-3 font-bold text-golden hover:bg-golden/10`

    6. Per D-03: Stage 5's third goal uses the EXACT text "Merlin graduates may be considered for affiliated aviation opportunities as the organization grows." — no cargo company name, no Cessna Caravan, no specifics beyond this.
  </action>
  <verify>
    <automated>test -f app/careers/page.tsx && grep -c "Stage" app/careers/page.tsx | xargs test 5 -le && echo "PASS: careers page exists with 5+ Stage references"</automated>
  </verify>
  <acceptance_criteria>
    - `app/careers/page.tsx` exists and is >= 100 lines
    - File contains `'use client'` directive
    - File contains `Fly Here. Get Hired Here.`
    - File contains all 5 stage titles: `Start Your Journey`, `Private Pilot Certificate`, `Instrument Rating`, `CFI Certificate`, `Career at Merlin`
    - File contains `IntersectionObserver` with `threshold`
    - File contains `activeIndex` state variable
    - File contains `bg-golden` (progress bar fill)
    - File contains `Merlin graduates may be considered for affiliated aviation opportunities` (D-03 exact text)
    - File does NOT contain any cargo company name, "Cessna Caravan", or specific cargo operations (D-03 prohibition)
    - File contains `Book Your Discovery Flight` and `See Training Timeline` CTAs
    - File contains metadata with `Pilot Career Path | Merlin Flight Training` (either inline or in layout.tsx)
  </acceptance_criteria>
  <done>/careers page created with 5-stage career pathway, scroll-driven active stage tracking, sticky sidebar with progress bar, and bottom CTAs. No prohibited cargo specifics.</done>
</task>

<task type="auto">
  <name>Task 2: Add career callouts to homepage, pricing, and discovery flight pages + verify LEAD-03 and PUB-03/PUB-04 no-regression</name>
  <files>
    app/page.tsx,
    app/pricing/page.tsx,
    app/discovery-flight/page.tsx
  </files>
  <read_first>
    app/page.tsx,
    app/pricing/page.tsx,
    app/discovery-flight/page.tsx,
    app/admin/tabs/ProspectsTab.tsx,
    app/blog/[slug]/page.tsx,
    app/components/SocialMediaFeed.tsx,
    .planning/phases/06-lead-nurturing-career-content/06-UI-SPEC.md,
    .planning/phases/06-lead-nurturing-career-content/06-CONTEXT.md
  </read_first>
  <action>
    **Part A: Career callouts (D-04, D-05, PUB-02)**

    Each callout is 1-2 sentences inline in an existing section, NOT a new standalone section. Use Next.js `Link` component for client-side navigation. Per UI-SPEC: `text-base text-gray-700`, link styled with `text-golden underline-offset-2 hover:underline`.

    1. **Homepage (`app/page.tsx`)** — Find the section near the instructor/training programs area (per RESEARCH: ~line 529 "Expert Instructors" feature card or ~line 562 "Flight Training Programs" section). Add BELOW the relevant content:
       ```tsx
       <p className="text-base text-gray-700 mt-4">
         Every Merlin student trains with an instructor who was once in your seat.
         The best candidates earn a spot on our team.{' '}
         <Link href="/careers" className="text-golden underline-offset-2 hover:underline">
           Learn about the career path &rarr;
         </Link>
       </p>
       ```
       IMPORTANT: Do NOT disturb the `SocialMediaFeed` component or any social feed section. Only add the callout near the instructor/training section. Read the full file to identify the exact insertion point.

    2. **Pricing (`app/pricing/page.tsx`)** — Find the CFI certification pricing tier section. Add near/below it:
       ```tsx
       <p className="text-base text-gray-700 mt-4">
         CFI certification isn&apos;t just a rating — it&apos;s your entry to a paid flying career.
         Merlin hires from within.{' '}
         <Link href="/careers" className="text-golden underline-offset-2 hover:underline">
           See how &rarr;
         </Link>
       </p>
       ```

    3. **Discovery flight (`app/discovery-flight/page.tsx`)** — Find the main CTA section (the bottom call-to-action area). Add ABOVE or BELOW the CTA button:
       ```tsx
       <p className="text-base text-gray-700 mt-4">
         Your first flight could be the start of a career.
         Merlin pilots train here and get hired here.{' '}
         <Link href="/careers" className="text-golden underline-offset-2 hover:underline">
           Learn more &rarr;
         </Link>
       </p>
       ```

    Ensure `Link` is imported from `next/link` in each file (likely already imported).

    **Part B: LEAD-03 gap verification**

    Read `app/admin/tabs/ProspectsTab.tsx` and verify:
    - `lead_stage` dropdown exists with values: new, contacted, booked, no-show, converted
    - `handleLeadStageChange` function calls `supabase.from('prospects').update({ lead_stage: ... })`
    - Color-coded badges exist for each stage

    If all present, LEAD-03 is confirmed met. If any gap found, fix it (unlikely per RESEARCH).

    **Part C: PUB-03 / PUB-04 regression check**

    1. Read `app/blog/[slug]/page.tsx` — confirm JSON-LD `Article` schema and `BreadcrumbList` schema are present. No changes needed.
    2. Read `app/components/SocialMediaFeed.tsx` — confirm it renders and handles empty state. No changes needed.
    3. Verify that the homepage edit in Part A does NOT remove or alter the `SocialMediaFeed` import or render call.

    Run TypeScript check: `npx tsc --noEmit --pretty 2>&1 | head -30`
  </action>
  <verify>
    <automated>grep -l "/careers" app/page.tsx app/pricing/page.tsx app/discovery-flight/page.tsx && grep "application/ld+json" app/blog/\[slug\]/page.tsx && grep "SocialMediaFeed" app/page.tsx && echo "PASS: callouts added, blog SEO present, social feed present"</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains `Learn about the career path` and `href="/careers"`
    - `app/page.tsx` still contains `SocialMediaFeed` import and render (PUB-04 no regression)
    - `app/pricing/page.tsx` contains `Merlin hires from within` and `href="/careers"`
    - `app/discovery-flight/page.tsx` contains `train here and get hired here` and `href="/careers"`
    - `app/blog/[slug]/page.tsx` still contains `application/ld+json` (PUB-03 no regression)
    - `app/admin/tabs/ProspectsTab.tsx` contains `handleLeadStageChange` and `lead_stage` dropdown values (LEAD-03 verified)
    - Each callout uses `text-golden` class for the link
    - No TypeScript errors from `tsc --noEmit`
  </acceptance_criteria>
  <done>Career callouts added to 3 pages. LEAD-03 verified as met by existing ProspectsTab. Blog SEO (PUB-03) and social feed (PUB-04) confirmed no regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of /careers page and career callouts</name>
  <files>app/careers/page.tsx, app/page.tsx, app/pricing/page.tsx, app/discovery-flight/page.tsx</files>
  <read_first>app/careers/page.tsx</read_first>
  <action>
    This is a human-verify checkpoint. The user visually confirms the /careers page and career callouts render correctly.

    Verification steps:
    1. Run `npm run dev` and open http://localhost:3000/careers
    2. Verify 5 career stages visible in order: Start Your Journey, Private Pilot Certificate, Instrument Rating, CFI Certificate, Career at Merlin
    3. Scroll down — left sidebar should update to show current stage, progress bar fills progressively
    4. Verify Stage 5 mentions "affiliated aviation opportunities" but does NOT name any specific cargo company
    5. Click "Book Your Discovery Flight" CTA — should navigate to /discovery-flight
    6. Click "See Training Timeline" CTA — should navigate to /private-pilot-timeline
    7. Visit http://localhost:3000 — find career callout near instructor/training section. Click "/careers" link.
    8. Visit http://localhost:3000/pricing — find career callout near CFI cert tier. Click "/careers" link.
    9. Visit http://localhost:3000/discovery-flight — find career callout near CTA. Click "/careers" link.
    10. Verify homepage social media feed section still renders (scroll to it)
  </action>
  <verify>
    <automated>echo "Human visual verification checkpoint — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms 5 career stages render correctly on /careers page
    - User confirms scroll tracking updates sidebar and progress bar
    - User confirms career callouts visible on homepage, pricing, and discovery flight pages
    - User confirms all /careers links navigate correctly
    - User confirms social media feed still renders on homepage
  </acceptance_criteria>
  <done>User has visually verified the /careers page and all career callouts across the site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor -> /careers page | Static content page, no user input, no data mutation |
| Public visitor -> callout links | Client-side navigation links only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | /careers page | accept | Page is intentionally public marketing content; no sensitive data exposed |
| T-06-11 | Tampering | app/page.tsx edit | mitigate | Edit is additive (insert callout text); SocialMediaFeed component and blog section are not touched; verified via grep in acceptance criteria |
| T-06-12 | Spoofing | /careers route | accept | No auth required — public content page by design (per Pitfall 6 in RESEARCH: verified /careers is NOT inside any authenticated layout wrapper) |
</threat_model>

<verification>
1. `/careers` page renders with 5 stages and scroll tracking
2. Career callouts exist on homepage, pricing, and discovery flight pages with /careers links
3. Blog JSON-LD schema unchanged (PUB-03)
4. SocialMediaFeed renders on homepage (PUB-04)
5. ProspectsTab lead_stage dropdown confirmed functional (LEAD-03)
6. Human visual verification passed
</verification>

<success_criteria>
- /careers page shows the full career pipeline from student to hired instructor
- Scroll tracking updates sidebar and progress bar
- "Train here, get hired here" callouts appear on 3 pages with working /careers links
- No regression on blog SEO or social media feed
- LEAD-03 confirmed met by existing admin UI
</success_criteria>

<output>
After completion, create `.planning/phases/06-lead-nurturing-career-content/06-03-SUMMARY.md`
</output>
