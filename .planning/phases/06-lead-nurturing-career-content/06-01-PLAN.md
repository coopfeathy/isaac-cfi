---
phase: 06-lead-nurturing-career-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260409_add_sequence_step.sql
  - app/api/discovery-flight-signup/route.ts
  - lib/resend.ts
  - netlify/functions/prospect-followup-day3.ts
  - netlify/functions/prospect-followup-day7.ts
  - app/api/discovery-flight-signup/__tests__/route.test.ts
  - netlify/functions/__tests__/prospect-followup-day3.test.ts
  - netlify/functions/__tests__/prospect-followup-day7.test.ts
autonomous: false
requirements:
  - LEAD-01
  - LEAD-02

user_setup:
  - service: supabase
    why: "Schema push for sequence_step column"
    env_vars: []
    dashboard_config:
      - task: "Run supabase db push or apply migration via Supabase dashboard if CLI auth fails"
        location: "Supabase Dashboard -> SQL Editor (if CLI push requires interactive auth)"

must_haves:
  truths:
    - "A new discovery flight form submission saves to prospects table with lead_stage='new' and sequence_step=0"
    - "A day-0 confirmation email is sent via Resend immediately after prospect insert"
    - "If Resend throws on day-0 email, the prospect row still exists in DB and the API returns 200"
    - "A Netlify scheduled function sends day-3 follow-up emails to prospects with sequence_step=1 and lead_stage in (new, contacted)"
    - "A Netlify scheduled function sends day-7 follow-up emails to prospects with sequence_step=2 and lead_stage in (new, contacted)"
    - "Follow-up functions are idempotent — prospects with sequence_step >= target are skipped"
  artifacts:
    - path: "supabase/migrations/20260409_add_sequence_step.sql"
      provides: "sequence_step column on prospects table"
      contains: "ALTER TABLE prospects"
    - path: "app/api/discovery-flight-signup/route.ts"
      provides: "Day-0 email send after DB insert"
      contains: "prospectWelcome"
    - path: "lib/resend.ts"
      provides: "Three new email templates: prospectWelcome, prospectFollowUpDay3, prospectFollowUpDay7"
      contains: "prospectFollowUpDay3"
    - path: "netlify/functions/prospect-followup-day3.ts"
      provides: "Scheduled function for day-3 follow-up"
      contains: "schedule"
    - path: "netlify/functions/prospect-followup-day7.ts"
      provides: "Scheduled function for day-7 follow-up"
      contains: "schedule"
  key_links:
    - from: "app/api/discovery-flight-signup/route.ts"
      to: "lib/resend.ts"
      via: "emailTemplates.prospectWelcome"
      pattern: "emailTemplates\\.prospectWelcome"
    - from: "netlify/functions/prospect-followup-day3.ts"
      to: "lib/resend.ts"
      via: "import emailTemplates"
      pattern: "prospectFollowUpDay3"
    - from: "netlify/functions/prospect-followup-day7.ts"
      to: "lib/resend.ts"
      via: "import emailTemplates"
      pattern: "prospectFollowUpDay7"
---

<objective>
Persist every discovery flight submission to the prospects table with sequence tracking, send a day-0 confirmation email via Resend, and create two Netlify scheduled functions that send day-3 and day-7 follow-up emails to non-converted prospects.

Purpose: Automates the prospect nurturing pipeline so Isaac never has to manually follow up with discovery flight leads.
Output: Migration file, updated signup route, three email templates, two scheduled functions, and full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lead-nurturing-career-content/06-CONTEXT.md
@.planning/phases/06-lead-nurturing-career-content/06-RESEARCH.md
@.planning/phases/06-lead-nurturing-career-content/06-VALIDATION.md

@app/api/discovery-flight-signup/route.ts
@lib/resend.ts
@netlify/functions/booking-monitor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration, email templates, and test scaffolds (Wave 0)</name>
  <files>
    supabase/migrations/20260409_add_sequence_step.sql,
    lib/resend.ts,
    app/api/discovery-flight-signup/__tests__/route.test.ts,
    netlify/functions/__tests__/prospect-followup-day3.test.ts,
    netlify/functions/__tests__/prospect-followup-day7.test.ts
  </files>
  <read_first>
    lib/resend.ts,
    app/api/discovery-flight-signup/route.ts,
    netlify/functions/booking-monitor.ts,
    supabase/migrations/20260408_add_lead_stage.sql,
    .planning/phases/06-lead-nurturing-career-content/06-RESEARCH.md,
    .planning/phases/06-lead-nurturing-career-content/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test: new prospect insert calls resend.emails.send with prospectWelcome template and updates sequence_step to 1
    - Test: when Resend throws, API still returns 200 and prospect row exists in DB (sequence_step remains 0)
    - Test: existing prospect (update path) does NOT trigger day-0 email
    - Test (day-3): prospects with sequence_step=1 and lead_stage in (new, contacted) and created_at ~3 days ago receive email and sequence_step is updated to 2
    - Test (day-3): prospects with sequence_step >= 2 are skipped
    - Test (day-3): prospects with lead_stage = 'converted' or 'booked' are skipped
    - Test (day-7): prospects with sequence_step=2 and lead_stage in (new, contacted) and created_at ~7 days ago receive email and sequence_step is updated to 3
    - Test (day-7): prospects with sequence_step >= 3 are skipped
  </behavior>
  <action>
    1. Create migration `supabase/migrations/20260409_add_sequence_step.sql`:
       ```sql
       ALTER TABLE prospects
         ADD COLUMN IF NOT EXISTS sequence_step int NOT NULL DEFAULT 0;
       COMMENT ON COLUMN prospects.sequence_step IS '0=no emails, 1=day-0 sent, 2=day-3 sent, 3=day-7 sent';
       ```

    2. Add three email templates to the `emailTemplates` export object in `lib/resend.ts`, following the existing pattern (each returns `{ subject, html }` using `emailWrapper()` and brand constants `brand.gold` = `#FFBF00`, `brand.dark` = `#0B0B0B`):

       - `prospectWelcome(name: string)` — Subject: "Your Discovery Flight is Confirmed — What Happens Next". Body: greeting with name, gold divider, what-to-expect list (instructor contact in 1-2 days, 60-90 min flight, no experience required), gold CTA button linking to `https://merlinflighttraining.com/careers` with text "See Where Training Can Take You", sign-off "Blue skies ahead, The Merlin Flight Training Team". Per D-07: confirmation + next-steps, NOT a hard sell.

       - `prospectFollowUpDay3(name: string)` — Subject: "Still thinking about flying, {name}?". Body: warm check-in, mention that discovery flights are filling up, link to book, brief mention of career path. Per D-06: warm tone, not high-pressure.

       - `prospectFollowUpDay7(name: string)` — Subject: "Your flight is waiting, {name}". Body: final gentle nudge, testimonial-style social proof line, direct booking link, mention that instructors are available this week. Per D-06: warm closing, not aggressive.

    3. Create test file `app/api/discovery-flight-signup/__tests__/route.test.ts`:
       - Mock `@/lib/supabase-admin` (getSupabaseAdmin) to return a mock supabase client
       - Mock `resend` from `lib/resend.ts`
       - Test cases per behavior block above (3 tests minimum)
       - All tests must FAIL initially (RED phase — production code not yet modified)

    4. Create `netlify/functions/__tests__/` directory if not exists.

    5. Create test file `netlify/functions/__tests__/prospect-followup-day3.test.ts`:
       - Mock `@supabase/supabase-js` createClient
       - Mock `resend` from `lib/resend.ts` (use `jest.mock` with relative path since Netlify functions don't use `@/` alias)
       - Test cases per behavior block above (3 tests minimum)

    6. Create test file `netlify/functions/__tests__/prospect-followup-day7.test.ts`:
       - Same mock pattern as day-3
       - Test cases per behavior block above (2 tests minimum)

    7. Run all three test files — they should all FAIL (tests reference production code not yet written).
  </action>
  <verify>
    <automated>npx jest app/api/discovery-flight-signup/__tests__/route.test.ts netlify/functions/__tests__/prospect-followup-day3.test.ts netlify/functions/__tests__/prospect-followup-day7.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260409_add_sequence_step.sql` contains `ALTER TABLE prospects` and `sequence_step`
    - `lib/resend.ts` contains `prospectWelcome:`, `prospectFollowUpDay3:`, and `prospectFollowUpDay7:`
    - `app/api/discovery-flight-signup/__tests__/route.test.ts` exists and contains `test(` or `it(`
    - `netlify/functions/__tests__/prospect-followup-day3.test.ts` exists and contains `sequence_step`
    - `netlify/functions/__tests__/prospect-followup-day7.test.ts` exists and contains `sequence_step`
    - Email templates each use `emailWrapper(` and reference `brand.gold` or `brand.dark`
  </acceptance_criteria>
  <done>Migration file created, three email templates added to lib/resend.ts, three test files created with failing tests covering all behavior cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prospect persistence, day-0 email, and follow-up functions (GREEN)</name>
  <files>
    app/api/discovery-flight-signup/route.ts,
    netlify/functions/prospect-followup-day3.ts,
    netlify/functions/prospect-followup-day7.ts
  </files>
  <read_first>
    app/api/discovery-flight-signup/route.ts,
    lib/resend.ts,
    netlify/functions/booking-monitor.ts,
    app/api/discovery-flight-signup/__tests__/route.test.ts,
    netlify/functions/__tests__/prospect-followup-day3.test.ts,
    netlify/functions/__tests__/prospect-followup-day7.test.ts
  </read_first>
  <behavior>
    - All tests from Task 1 pass (GREEN)
    - discovery-flight-signup/route.ts: on INSERT path, sends day-0 email via emailTemplates.prospectWelcome, then updates sequence_step to 1. On email failure, logs error, does NOT return error to client.
    - prospect-followup-day3.ts: queries prospects where sequence_step < 2 AND lead_stage IN ('new','contacted') AND created_at in 3-day window, sends email, updates sequence_step to 2
    - prospect-followup-day7.ts: queries prospects where sequence_step < 3 AND lead_stage IN ('new','contacted') AND created_at in 7-day window, sends email, updates sequence_step to 3
  </behavior>
  <action>
    1. Modify `app/api/discovery-flight-signup/route.ts`:
       - Import `Resend` from `resend` and `emailTemplates` from `@/lib/resend`
       - After the existing INSERT path (where `existingProspect` is null/undefined and the `.insert()` or `.upsert()` succeeds), add a try/catch block:
         ```typescript
         // Day-0 confirmation email — per D-09: email failure must NOT affect response
         try {
           const resend = new Resend(process.env.RESEND_API_KEY)
           await resend.emails.send({
             from: 'Merlin Flight Training <noreply@merlinflighttraining.com>',
             to: [email],
             subject: emailTemplates.prospectWelcome(name).subject,
             html: emailTemplates.prospectWelcome(name).html,
           })
           await supabase.from('prospects').update({ sequence_step: 1 }).eq('email', email)
         } catch (emailErr) {
           console.error('Day-0 email failed (prospect saved):', emailErr)
         }
         ```
       - On the UPDATE path (existing prospect), do NOT send any email per Pitfall 1 in RESEARCH.md
       - Ensure the insert includes `lead_stage: 'new'` explicitly (even though DB default handles it)

    2. Create `netlify/functions/prospect-followup-day3.ts`:
       - Follow `booking-monitor.ts` pattern exactly: import `Config, Handler` from `@netlify/functions`, import `createClient` from `@supabase/supabase-js`
       - Import `Resend` from `resend` and import `emailTemplates` — use relative path `../../lib/resend` (Netlify functions don't support `@/` path alias)
       - Config: `schedule: '0 14 * * *'` (daily 2pm UTC / 10am ET)
       - Handler logic:
         a. Create supabaseAdmin with `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`
         b. Calculate 3-day window: `threeDaysAgo = Date.now() - 3*24*60*60*1000`, windowStart = -2h, windowEnd = +2h
         c. Query: `supabaseAdmin.from('prospects').select('id, email, full_name, lead_stage, sequence_step').gte('created_at', windowStart.toISOString()).lte('created_at', windowEnd.toISOString()).lt('sequence_step', 2).in('lead_stage', ['new', 'contacted'])`
         d. For each prospect: send email using `emailTemplates.prospectFollowUpDay3(prospect.full_name || 'there')`, then update `sequence_step` to 2
         e. Log count sent, return `{ statusCode: 200, body: JSON.stringify({ sent: count }) }`

    3. Create `netlify/functions/prospect-followup-day7.ts`:
       - Same pattern as day-3, but:
         a. Calculate 7-day window instead of 3-day
         b. Query filter: `.lt('sequence_step', 3)` and same `lead_stage` filter
         c. Send `emailTemplates.prospectFollowUpDay7(prospect.full_name || 'there')`
         d. Update `sequence_step` to 3

    4. Run all tests — they should now PASS.
  </action>
  <verify>
    <automated>npx jest app/api/discovery-flight-signup/__tests__/route.test.ts netlify/functions/__tests__/prospect-followup-day3.test.ts netlify/functions/__tests__/prospect-followup-day7.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/discovery-flight-signup/route.ts` contains `emailTemplates.prospectWelcome`
    - `app/api/discovery-flight-signup/route.ts` contains `sequence_step: 1` update after email send
    - `app/api/discovery-flight-signup/route.ts` contains `catch (emailErr)` with console.error (per D-09)
    - `netlify/functions/prospect-followup-day3.ts` contains `schedule: '0 14 * * *'`
    - `netlify/functions/prospect-followup-day3.ts` contains `.lt('sequence_step', 2)`
    - `netlify/functions/prospect-followup-day3.ts` contains `in('lead_stage', ['new', 'contacted'])`
    - `netlify/functions/prospect-followup-day7.ts` contains `.lt('sequence_step', 3)`
    - All 3 test files pass with exit code 0
  </acceptance_criteria>
  <done>Discovery flight signup route sends day-0 email after DB insert (failure-safe). Two Netlify scheduled functions send day-3 and day-7 follow-ups to non-converted prospects. All tests pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Push sequence_step migration to Supabase</name>
  <files>supabase/migrations/20260409_add_sequence_step.sql</files>
  <read_first>supabase/migrations/20260409_add_sequence_step.sql</read_first>
  <action>
    This is a human-action checkpoint. The user must apply the migration to their live Supabase database.

    Option A — CLI push:
    ```bash
    supabase db push
    ```

    Option B — Manual SQL (if CLI auth fails or requires interactive prompt):
    1. Open Supabase Dashboard -> SQL Editor
    2. Paste and run: `ALTER TABLE prospects ADD COLUMN IF NOT EXISTS sequence_step int NOT NULL DEFAULT 0;`

    Verification query (run in SQL Editor):
    ```sql
    SELECT column_name, data_type, column_default
    FROM information_schema.columns
    WHERE table_name = 'prospects' AND column_name = 'sequence_step';
    ```
    Expected: one row with `integer` type and `0` default.
  </action>
  <verify>
    <automated>echo "Human checkpoint — verify via Supabase Dashboard SQL Editor query"</automated>
  </verify>
  <acceptance_criteria>
    - Supabase `prospects` table has a `sequence_step` column of type `integer` with default `0`
    - User has confirmed "pushed" in the resume signal
  </acceptance_criteria>
  <done>The sequence_step column exists in the live Supabase database and is ready for the follow-up email sequence functions to read and write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> discovery-flight-signup API | Untrusted form input (email, name) from public internet |
| Scheduled function -> Supabase | Service role key grants full DB access |
| Scheduled function -> Resend | API key allows sending email as Merlin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | discovery-flight-signup | mitigate | Email validated before insert (existing validation preserved); rate limiting added in Plan 02 |
| T-06-02 | Denial of Service | discovery-flight-signup | mitigate | Rate limiting (10/IP/hour) added in Plan 02; this plan ensures DB write is resilient to email failure |
| T-06-03 | Information Disclosure | prospect-followup-day3/7 | mitigate | Resend API key and Supabase service role key stored in server-side env vars only; never in client bundle |
| T-06-04 | Repudiation | prospect-followup-day3/7 | accept | Follow-up sends are logged via console; Resend dashboard provides delivery audit trail; low-value target |
| T-06-05 | Tampering | sequence_step column | mitigate | Only server-side code updates sequence_step; no client-facing endpoint exposes write access to this column |
</threat_model>

<verification>
1. `npx jest --testPathPattern="discovery-flight-signup|prospect-followup" --no-coverage` — all tests pass
2. `supabase/migrations/20260409_add_sequence_step.sql` exists with correct ALTER TABLE
3. `lib/resend.ts` exports three new email templates
4. `netlify/functions/prospect-followup-day3.ts` and `prospect-followup-day7.ts` exist with Config.schedule
5. Schema push confirmed by human checkpoint
</verification>

<success_criteria>
- All discovery flight form submissions persist to prospects with sequence_step=0
- Day-0 email fires on new prospect insert; email failure does not affect API response
- Day-3 scheduled function sends to prospects with sequence_step < 2 and non-converted lead_stage
- Day-7 scheduled function sends to prospects with sequence_step < 3 and non-converted lead_stage
- All automated tests pass
- sequence_step column exists in live Supabase database
</success_criteria>

<output>
After completion, create `.planning/phases/06-lead-nurturing-career-content/06-01-SUMMARY.md`
</output>
