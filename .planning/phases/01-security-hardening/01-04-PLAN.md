---
phase: 01-security-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - app/api/admin/billing/checkout/route.ts
  - app/api/admin/billing/send-reminder/route.ts
  - app/api/admin/billing/sync-products/route.ts
  - app/api/admin/billing/accountant-text/route.ts
  - app/api/admin/billing/cash-payment/route.ts
  - app/api/admin/billing/delete-checkout/route.ts
  - app/api/admin/billing/overview/route.ts
  - app/api/admin/billing/payout-rules/route.ts
  - app/api/admin/billing/push-checkout-link/route.ts
  - app/api/admin/availability/route.ts
  - app/api/admin/availability-overrides/route.ts
  - app/api/admin/bookings/manual/route.ts
  - app/api/admin/caldav/push/route.ts
  - app/api/admin/caldav/settings/route.ts
  - app/api/admin/caldav/sync/route.ts
  - app/api/admin/caldav/test/route.ts
  - app/api/admin/enrollments/migrate-course/route.ts
  - app/api/admin/prospects/[prospectId]/route.ts
  - app/api/admin/slot-requests/[id]/approve/route.ts
  - app/api/admin/slot-requests/[id]/deny/route.ts
  - app/api/admin/students/normalize/route.ts
  - app/api/admin/students/route.ts
  - app/api/admin/students/send-account-link/route.ts
  - app/booking/page.tsx
autonomous: false
requirements:
  - SEC-04
  - SEC-09

must_haves:
  truths:
    - "All 23 admin API routes import requireAdmin from @/lib/auth (no local inline copies remain)"
    - "Zero files contain 'async function requireAdmin' as a local definition"
    - "/booking redirects to /schedule"
    - "The old booking page component is replaced with a redirect-only stub"
  artifacts:
    - path: "app/booking/page.tsx"
      provides: "Redirect stub to /schedule"
      contains: "redirect"
  key_links:
    - from: "all 23 app/api/admin/* routes"
      to: "lib/auth.ts"
      via: "import { requireAdmin }"
      pattern: "import.*requireAdmin.*from.*@/lib/auth"
---

<objective>
Replace all 23 inline requireAdmin() copies across admin API routes with imports from lib/auth.ts, and retire the legacy /booking route with a redirect to /schedule.

Purpose: This is the mechanical completion of SEC-04 — every admin API route now uses the canonical auth guard from lib/auth.ts instead of its own inline copy. The /booking redirect (SEC-09) closes the last legacy route gap.

Output: 23 admin API routes using imported requireAdmin, /booking redirecting to /schedule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@.planning/phases/01-security-hardening/01-01-SUMMARY.md
@.planning/phases/01-security-hardening/01-02-SUMMARY.md
@.planning/phases/01-security-hardening/01-03-SUMMARY.md

<interfaces>
<!-- From lib/auth.ts (created in Plan 01): -->
```typescript
export async function requireAdmin(request: NextRequest): Promise<
  { error: NextResponse } | { user: User; profile: { is_admin: boolean } }
>
```

Usage pattern (replaces the local function in each file):
```typescript
// REMOVE this local function:
// async function requireAdmin(request: NextRequest) { ... }

// ADD this import at top of file:
import { requireAdmin } from '@/lib/auth'

// Usage in handler remains the same:
const adminCheck = await requireAdmin(request)
if ('error' in adminCheck) return adminCheck.error
const { user } = adminCheck
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline requireAdmin in all 23 admin API routes with import from lib/auth</name>
  <files>
    app/api/admin/billing/checkout/route.ts,
    app/api/admin/billing/send-reminder/route.ts,
    app/api/admin/billing/sync-products/route.ts,
    app/api/admin/billing/accountant-text/route.ts,
    app/api/admin/billing/cash-payment/route.ts,
    app/api/admin/billing/delete-checkout/route.ts,
    app/api/admin/billing/overview/route.ts,
    app/api/admin/billing/payout-rules/route.ts,
    app/api/admin/billing/push-checkout-link/route.ts,
    app/api/admin/availability/route.ts,
    app/api/admin/availability-overrides/route.ts,
    app/api/admin/bookings/manual/route.ts,
    app/api/admin/caldav/push/route.ts,
    app/api/admin/caldav/settings/route.ts,
    app/api/admin/caldav/sync/route.ts,
    app/api/admin/caldav/test/route.ts,
    app/api/admin/enrollments/migrate-course/route.ts,
    app/api/admin/prospects/[prospectId]/route.ts,
    app/api/admin/slot-requests/[id]/approve/route.ts,
    app/api/admin/slot-requests/[id]/deny/route.ts,
    app/api/admin/students/normalize/route.ts,
    app/api/admin/students/route.ts,
    app/api/admin/students/send-account-link/route.ts
  </files>
  <read_first>
    - lib/auth.ts (the canonical import source — verify exports)
    - app/api/admin/billing/checkout/route.ts (canonical reference — this is the file the pattern was extracted from per D-04)
    - app/api/admin/students/route.ts (spot-check a second file to confirm pattern consistency)
    - app/api/admin/caldav/sync/route.ts (spot-check a third file)
  </read_first>
  <action>
For each of the 23 files listed below, make exactly two changes:

**Change 1 — Add import:** Add `import { requireAdmin } from '@/lib/auth'` to the import block at the top of the file. If the file already imports from `@/lib/auth` (unlikely but possible), add `requireAdmin` to the existing import.

**Change 2 — Remove local function:** Delete the entire `async function requireAdmin(request: NextRequest) { ... }` local function definition. This function typically spans 15-20 lines and includes the inline auth check pattern. Remove it completely — the imported version replaces it.

Do NOT change the call sites — the usage pattern (`const adminCheck = await requireAdmin(request); if ('error' in adminCheck) return adminCheck.error`) is identical between the local and imported versions.

**If any file has a slightly different local requireAdmin signature** (e.g., different return shape, different error messages), still replace it with the import. The canonical version from lib/auth.ts is authoritative.

**If any file also imports `supabase` from `@/lib/supabase` solely for the local requireAdmin's `getUser` call** (and doesn't use `supabase` elsewhere in the handler), remove that import too to avoid unused import warnings. Check each file — most files use `supabase` or `getSupabaseAdmin` for other purposes, so only remove the import if it's truly orphaned.

The 23 files to edit:
1. `app/api/admin/billing/checkout/route.ts`
2. `app/api/admin/billing/send-reminder/route.ts`
3. `app/api/admin/billing/sync-products/route.ts`
4. `app/api/admin/billing/accountant-text/route.ts`
5. `app/api/admin/billing/cash-payment/route.ts`
6. `app/api/admin/billing/delete-checkout/route.ts`
7. `app/api/admin/billing/overview/route.ts`
8. `app/api/admin/billing/payout-rules/route.ts`
9. `app/api/admin/billing/push-checkout-link/route.ts`
10. `app/api/admin/availability/route.ts`
11. `app/api/admin/availability-overrides/route.ts`
12. `app/api/admin/bookings/manual/route.ts`
13. `app/api/admin/caldav/push/route.ts`
14. `app/api/admin/caldav/settings/route.ts`
15. `app/api/admin/caldav/sync/route.ts`
16. `app/api/admin/caldav/test/route.ts`
17. `app/api/admin/enrollments/migrate-course/route.ts`
18. `app/api/admin/prospects/[prospectId]/route.ts`
19. `app/api/admin/slot-requests/[id]/approve/route.ts`
20. `app/api/admin/slot-requests/[id]/deny/route.ts`
21. `app/api/admin/students/normalize/route.ts`
22. `app/api/admin/students/route.ts`
23. `app/api/admin/students/send-account-link/route.ts`

**Post-edit verification command:**
```bash
# Must return 0 — no local requireAdmin definitions remain
grep -rl "async function requireAdmin" app/api/ | wc -l

# Must return 23 — all files import from lib/auth
grep -rl "import.*requireAdmin.*from.*@/lib/auth" app/api/admin/ | wc -l
```
  </action>
  <verify>
    <automated>echo "Local copies:" && grep -rl "async function requireAdmin" app/api/ | wc -l && echo "Imports:" && grep -rl "import.*requireAdmin.*from.*@/lib/auth" app/api/admin/ | wc -l && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "async function requireAdmin" app/api/` returns 0 files (zero local copies remain anywhere in app/api/)
    - `grep -rl "import.*requireAdmin.*from.*@/lib/auth" app/api/admin/` returns 23 files
    - `npx jest` exits 0 (full suite — existing tests + new auth tests all pass)
    - No TypeScript compilation errors from orphaned imports (run `npx tsc --noEmit` if available)
  </acceptance_criteria>
  <done>All 23 admin API routes import requireAdmin from @/lib/auth. Zero local requireAdmin function definitions remain in any API route file. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Retire /booking route with redirect to /schedule</name>
  <files>app/booking/page.tsx</files>
  <read_first>
    - app/booking/page.tsx (current legacy booking page — full multi-step form calling deprecated Netlify function)
  </read_first>
  <action>
Replace the entire contents of `app/booking/page.tsx` with a redirect stub per SEC-09:

```typescript
import { redirect } from 'next/navigation'

export default function BookingPage() {
  redirect('/schedule')
}
```

This is a Server Component (no `'use client'` directive). Next.js `redirect()` in App Router returns a 307 redirect during development and handles the redirect server-side in production.

Delete all existing imports, state, form logic, and JSX from the current file. The entire multi-step booking form is dead code that calls a deprecated Netlify function (`/.netlify/functions/book`).

Do NOT delete the `app/booking/` directory or `layout.tsx` if one exists — only replace `page.tsx`.
  </action>
  <verify>
    <automated>grep -c "redirect('/schedule')" app/booking/page.tsx && wc -l < app/booking/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/booking/page.tsx contains `import { redirect } from 'next/navigation'`
    - app/booking/page.tsx contains `redirect('/schedule')`
    - app/booking/page.tsx is fewer than 10 lines total
    - app/booking/page.tsx does NOT contain `'use client'`
    - app/booking/page.tsx does NOT contain `useState` or `useEffect`
    - app/booking/page.tsx does NOT reference `/.netlify/functions/book`
  </acceptance_criteria>
  <done>/booking route replaced with a server-side redirect to /schedule. All dead booking form code removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 1 security hardening end-to-end</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 1 security hardening by testing server-side redirects in a browser and confirming Supabase/Netlify setup steps.
  </action>
  <verify>Human confirms all verification steps pass</verify>
  <done>Phase 1 security hardening verified end-to-end</done>
  <what-built>
    Complete Phase 1 security hardening:
    1. lib/auth.ts with requireAdmin(), requireCFI(), requireUser()
    2. middleware.ts with server-side session refresh for /admin/* and /manage/*
    3. /manage layout converted to Server Component with admin role guard
    4. NEXT_PUBLIC_ADMIN_EMAIL removed from AuthContext
    5. create-user API guarded with requireAdmin
    6. upload-image route: auth + MIME + size + Supabase Storage
    7. Contact form: 503 + DB fallback when email unavailable
    8. All 23 inline requireAdmin copies replaced with imports
    9. /booking redirects to /schedule
    10. All PII console.log calls removed
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the browser
    2. Navigate to `/manage/users` without being logged in — verify you are redirected to `/login` (not a blank page or client-side flicker)
    3. Log in as a non-admin user and navigate to `/manage/users` — verify you are redirected to `/dashboard`
    4. Navigate to `/booking` — verify you are redirected to `/schedule`
    5. Run `grep -r 'NEXT_PUBLIC_ADMIN_EMAIL' app/ lib/` — verify zero matches
    6. Run `grep -rn 'console.log' app/api/create-user/ app/api/contact/ app/contexts/AuthContext.tsx | grep -i 'email\|phone\|sign\|profileData\|CONTACT FORM'` — verify zero PII log matches
    7. Run `npx jest` — verify all tests pass
    8. **Supabase setup check:** Confirm `contact_submissions` table exists in Supabase (run migration SQL if not yet applied). Confirm `blog-images` bucket exists in Supabase Storage (create via dashboard if needed).
    9. **Netlify env var:** Remove `NEXT_PUBLIC_ADMIN_EMAIL` from Netlify dashboard environment variables (Settings > Environment Variables).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> 23 admin API routes | All admin routes now use centralized auth guard from lib/auth.ts |
| browser -> /booking | Legacy route that should no longer be accessible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | Elevation of Privilege | 23 admin API routes (SEC-04) | mitigate | All 23 files import `requireAdmin` from `@/lib/auth` — centralized, tested, consistent. No local copies remain that could drift or have bugs. |
| T-1-12 | Tampering | /booking legacy route (SEC-09) | mitigate | Replace entire booking form with `redirect('/schedule')` — deprecated Netlify function is unreachable. |
| T-1-13 | Repudiation | Orphaned local requireAdmin | mitigate | Post-edit grep verification ensures zero local `async function requireAdmin` definitions remain — prevents shadow functions that bypass the canonical implementation. |
</threat_model>

<verification>
1. `grep -rl "async function requireAdmin" app/api/ | wc -l` — returns 0
2. `grep -rl "import.*requireAdmin.*from.*@/lib/auth" app/api/admin/ | wc -l` — returns 23
3. `grep -c "redirect('/schedule')" app/booking/page.tsx` — returns 1
4. `npx jest` — full suite passes
5. `grep -r 'NEXT_PUBLIC_ADMIN_EMAIL' app/ lib/` — returns no matches (full phase verification)
6. `grep -rn 'console.log.*email\|console.log.*phone' app/api/ app/contexts/` — returns no PII matches
</verification>

<success_criteria>
- All 23 admin API routes use imported requireAdmin from lib/auth.ts
- Zero local requireAdmin function definitions remain anywhere in app/api/
- /booking redirects to /schedule
- Full test suite passes
- Human verification confirms server-side redirects work correctly in browser
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-04-SUMMARY.md`
</output>
