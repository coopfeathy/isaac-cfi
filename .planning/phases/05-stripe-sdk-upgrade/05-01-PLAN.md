---
phase: 05-stripe-sdk-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - netlify/functions/stripe-webhook.ts
  - netlify/functions/create-payment-intent.ts
  - netlify/functions/create-checkout.ts
  - app/api/stripe-webhook/route.ts
  - app/api/create-payment-intent/route.ts
  - app/api/admin/billing/accountant-text/route.ts
  - app/api/admin/billing/send-reminder/route.ts
  - app/api/admin/billing/sync-products/route.ts
  - app/api/admin/billing/invoice/route.ts
  - app/api/admin/billing/checkout/route.ts
  - app/api/admin/billing/overview/route.ts
  - app/api/admin/billing/delete-checkout/route.ts
  - app/api/admin/billing/push-checkout-link/route.ts
  - app/api/student/setup-intent/route.ts
  - app/api/student/bookings/[id]/cancel/route.ts
  - app/api/student/billing-portal/route.ts
  - app/api/student/invoices/route.ts
autonomous: true
requirements:
  - STRIPE-01
  - STRIPE-02
  - STRIPE-03
  - STRIPE-04

must_haves:
  truths:
    - "Dead Netlify Stripe functions no longer exist in the repository"
    - "stripe server package is version 17.x"
    - "All 14 active Stripe files use apiVersion '2025-02-24.acacia'"
    - "No file in app/ or lib/ contains apiVersion '2022-11-15'"
    - "Webhook handler still uses req.text() for signature verification"
    - "TypeScript compiles without errors after the upgrade"
  artifacts:
    - path: "package.json"
      provides: "stripe@^17.7.0 dependency"
      contains: '"stripe": "^17.7.0"'
    - path: "app/api/stripe-webhook/route.ts"
      provides: "Webhook handler with correct apiVersion and req.text()"
      contains: "2025-02-24.acacia"
  key_links:
    - from: "package.json"
      to: "all 14 route files"
      via: "stripe SDK import"
      pattern: "new Stripe.*apiVersion.*2025-02-24.acacia"
---

<objective>
Upgrade the Stripe server SDK from 12.8.0 to 17.7.0, delete the 3 dead Netlify functions (per D-01), and update apiVersion from '2022-11-15' to '2025-02-24.acacia' in all 14 active server files. Verify TypeScript compiles clean.

Purpose: Bring the server-side Stripe integration to the current stable SDK version. The old 2022-11-15 API version is 3+ years behind; v17 provides better retry defaults (2 retries, 5s timeout) and access to the acacia API family.

Output: All server Stripe files running on v17 SDK with current apiVersion; dead Netlify functions removed; clean TypeScript build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stripe-sdk-upgrade/05-CONTEXT.md
@.planning/phases/05-stripe-sdk-upgrade/05-RESEARCH.md

<interfaces>
<!-- All 14 active server files use the same Stripe constructor pattern. -->
<!-- Two patterns exist: direct instantiation and conditional instantiation. -->

Pattern A — Direct (used in webhook, create-payment-intent, student routes, invoice, stripe-customer):
```typescript
const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
  apiVersion: '2022-11-15',
})
```

Pattern B — Conditional (used in most admin billing routes):
```typescript
const stripe = process.env.STRIPE_SECRET_KEY
  ? new Stripe(process.env.STRIPE_SECRET_KEY, { apiVersion: '2022-11-15' })
  : null
```

Both patterns: only the apiVersion string changes. Constructor signature is identical v12 to v17.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead Netlify functions and install stripe@17.7.0</name>
  <read_first>
    - netlify/functions/stripe-webhook.ts
    - netlify/functions/create-payment-intent.ts
    - netlify/functions/create-checkout.ts
    - package.json
  </read_first>
  <files>
    netlify/functions/stripe-webhook.ts
    netlify/functions/create-payment-intent.ts
    netlify/functions/create-checkout.ts
    package.json
    package-lock.json
  </files>
  <action>
STEP 1 — Delete the 3 dead Netlify functions (per D-01, STRIPE-01):
```bash
rm netlify/functions/stripe-webhook.ts
rm netlify/functions/create-payment-intent.ts
rm netlify/functions/create-checkout.ts
```

STEP 2 — Install the new Stripe server SDK (per D-06, on isolated branch):
```bash
npm install stripe@^17.7.0
```

This MUST happen BEFORE editing any source files. The Stripe SDK ships TypeScript types that define `apiVersion` as a union of valid string literals. If source files are edited before the new SDK is installed, TypeScript will reject `'2025-02-24.acacia'` as an invalid literal because the old v12 types don't include it.

IMPORTANT: If `npm install` warns about peer dependency conflicts, note them but proceed. The only peer dep concern is between `@stripe/react-stripe-js` and `@stripe/stripe-js` — those are handled in Plan 02.

Verify the installed version:
```bash
node -e "console.log(require('./node_modules/stripe/package.json').version)"
```
Should output a version starting with `17.`.

NOTE on retry/timeout defaults: v17 changes defaults from 1 retry / 2s timeout to 2 retries / 5s timeout. This is a safe improvement — no code changes needed for this.
  </action>
  <verify>
    <automated>ls netlify/functions/stripe-webhook.ts 2>/dev/null && echo "FAIL: file still exists" || echo "PASS: deleted" && ls netlify/functions/create-payment-intent.ts 2>/dev/null && echo "FAIL: file still exists" || echo "PASS: deleted" && ls netlify/functions/create-checkout.ts 2>/dev/null && echo "FAIL: file still exists" || echo "PASS: deleted" && node -e "const v = require('./node_modules/stripe/package.json').version; console.log(v); process.exit(v.startsWith('17.') ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `netlify/functions/stripe-webhook.ts` does not exist
    - `netlify/functions/create-payment-intent.ts` does not exist
    - `netlify/functions/create-checkout.ts` does not exist
    - `package.json` contains `"stripe": "^17.7.0"` (or a compatible ^17.x range)
    - `node -e "require('./node_modules/stripe/package.json').version"` outputs a version starting with `17.`
    - Other Netlify functions (`book.ts`, `caldav-sync.ts`, `generate-discovery-slots.ts`, `homework-email-dispatcher.ts`, `booking-monitor.ts`) are NOT deleted — only the 3 dead Stripe files
  </acceptance_criteria>
  <done>3 dead Netlify functions deleted; stripe@17.x installed and resolvable in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Update apiVersion to '2025-02-24.acacia' in all 14 active server files</name>
  <read_first>
    - app/api/stripe-webhook/route.ts
    - app/api/create-payment-intent/route.ts
    - app/api/admin/billing/accountant-text/route.ts
    - app/api/admin/billing/send-reminder/route.ts
    - app/api/admin/billing/sync-products/route.ts
    - app/api/admin/billing/invoice/route.ts
    - app/api/admin/billing/checkout/route.ts
    - app/api/admin/billing/overview/route.ts
    - app/api/admin/billing/delete-checkout/route.ts
    - app/api/admin/billing/push-checkout-link/route.ts
    - app/api/student/setup-intent/route.ts
    - app/api/student/bookings/[id]/cancel/route.ts
    - app/api/student/billing-portal/route.ts
    - app/api/student/invoices/route.ts
  </read_first>
  <files>
    app/api/stripe-webhook/route.ts
    app/api/create-payment-intent/route.ts
    app/api/admin/billing/accountant-text/route.ts
    app/api/admin/billing/send-reminder/route.ts
    app/api/admin/billing/sync-products/route.ts
    app/api/admin/billing/invoice/route.ts
    app/api/admin/billing/checkout/route.ts
    app/api/admin/billing/overview/route.ts
    app/api/admin/billing/delete-checkout/route.ts
    app/api/admin/billing/push-checkout-link/route.ts
    app/api/student/setup-intent/route.ts
    app/api/student/bookings/[id]/cancel/route.ts
    app/api/student/billing-portal/route.ts
    app/api/student/invoices/route.ts
  </files>
  <action>
In each of the 14 files listed below, find the Stripe constructor and replace the apiVersion string. The change is identical in every file:

**FIND:** `apiVersion: '2022-11-15'`
**REPLACE WITH:** `apiVersion: '2025-02-24.acacia'`

The 14 files to update (STRIPE-02, STRIPE-03):

1. `app/api/stripe-webhook/route.ts` — direct pattern
2. `app/api/create-payment-intent/route.ts` — direct pattern
3. `app/api/admin/billing/accountant-text/route.ts` — conditional pattern
4. `app/api/admin/billing/send-reminder/route.ts` — conditional pattern
5. `app/api/admin/billing/sync-products/route.ts` — conditional pattern
6. `app/api/admin/billing/invoice/route.ts` — direct pattern
7. `app/api/admin/billing/checkout/route.ts` — conditional pattern
8. `app/api/admin/billing/overview/route.ts` — conditional pattern
9. `app/api/admin/billing/delete-checkout/route.ts` — direct pattern
10. `app/api/admin/billing/push-checkout-link/route.ts` — conditional pattern
11. `app/api/student/setup-intent/route.ts` — direct pattern
12. `app/api/student/bookings/[id]/cancel/route.ts` — direct pattern
13. `app/api/student/billing-portal/route.ts` — direct pattern
14. `app/api/student/invoices/route.ts` — direct pattern

NOTE on `lib/stripe-customer.ts`: This file does NOT instantiate Stripe directly — verified by reading the file. The `ensureStripeCustomer()` function signature is `ensureStripeCustomer(stripe: Stripe, studentId: string, ...)` — it receives a pre-constructed Stripe instance as a parameter. There is no `new Stripe()` call and no `apiVersion` string in this file. It inherits whatever API version the calling route configured. No update needed. (RESEARCH.md Pitfall 2 originally flagged this file incorrectly; corrected during plan revision.)

NOTE on `lib/stripe-connect.ts`: This file does NOT instantiate Stripe directly. It does NOT need an apiVersion update.

TYPE SAFETY NOTE (Open Question from RESEARCH): If TypeScript rejects `'2025-02-24.acacia'` as not matching the `Stripe.LatestApiVersion` type, check `node_modules/stripe/types/index.d.ts` for the valid `LatestApiVersion` union. If `2025-02-24.acacia` is not in the union, use `as Stripe.LatestApiVersion` cast — but ONLY as a last resort. First try: is the string accepted? If not, check if a different acacia version is the latest in 17.7.0 types.

After all 14 files are updated, verify (STRIPE-04 / D-05):
- `app/api/stripe-webhook/route.ts` still contains `req.text()` (NOT `req.json()`) for the webhook body reading
- The `stripe.webhooks.constructEvent(body, signature, webhookSecret)` call is unchanged

Then run the build to catch any TypeScript errors:
```bash
npx tsc --noEmit
```

If TypeScript errors appear, they will be one of:
- `apiVersion` string literal not in union → see TYPE SAFETY NOTE above
- Removed/renamed properties on Stripe types → check RESEARCH.md breaking changes section; the codebase does NOT use any of the removed APIs (Billing.Alert, Terminal.Reader, Subscriptions.del) so this is unlikely
- Any other type error → read the error, fix the specific type issue

After `tsc --noEmit` passes, run the full build:
```bash
npm run build
```
  </action>
  <verify>
    <automated>grep -r "2022-11-15" app/ lib/ --include="*.ts" | wc -l | xargs -I {} test {} -eq 0 && echo "PASS: no old apiVersion" || echo "FAIL: old apiVersion still present" && grep -c "2025-02-24.acacia" app/api/stripe-webhook/route.ts app/api/create-payment-intent/route.ts app/api/admin/billing/accountant-text/route.ts app/api/admin/billing/send-reminder/route.ts app/api/admin/billing/sync-products/route.ts app/api/admin/billing/invoice/route.ts app/api/admin/billing/checkout/route.ts app/api/admin/billing/overview/route.ts app/api/admin/billing/delete-checkout/route.ts app/api/admin/billing/push-checkout-link/route.ts app/api/student/setup-intent/route.ts app/api/student/bookings/\[id\]/cancel/route.ts app/api/student/billing-portal/route.ts app/api/student/invoices/route.ts && grep "req.text()" app/api/stripe-webhook/route.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "2022-11-15" app/ lib/ --include="*.ts"` returns zero matches
    - `grep -c "2025-02-24.acacia"` returns at least 1 match in each of the 14 files listed above
    - `grep "req.text()" app/api/stripe-webhook/route.ts` returns a match (STRIPE-04 confirmed)
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 14 active Stripe server files use apiVersion '2025-02-24.acacia'; zero instances of '2022-11-15' remain in app/ or lib/; webhook handler confirmed using req.text(); TypeScript and Next.js build pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook → app | External HTTP POST with signature; must verify HMAC |
| App → Stripe API | Outbound API calls with secret key; key must stay in env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | stripe-webhook/route.ts | mitigate | Webhook signature verification via `stripe.webhooks.constructEvent()` with `req.text()` body — already implemented, verified unchanged by this upgrade (STRIPE-04) |
| T-05-02 | Tampering | package.json | mitigate | Pin `stripe@^17.7.0` (not `*` or `latest`); commit `package-lock.json` to prevent supply chain drift |
| T-05-03 | Information Disclosure | All route files | mitigate | `STRIPE_SECRET_KEY` stays in `process.env` — never committed to source; no new env vars introduced |
| T-05-04 | Denial of Service | Stripe API calls | accept | v17 increases default timeout from 2s to 5s and retries from 1 to 2 — slightly longer worst-case latency but improves resilience; acceptable tradeoff |
</threat_model>

<verification>
1. `grep -r "2022-11-15" app/ lib/ --include="*.ts" | wc -l` returns 0
2. `grep -r "2025-02-24.acacia" app/ lib/ --include="*.ts" | wc -l` returns 14
3. `ls netlify/functions/stripe-webhook.ts 2>/dev/null` returns nothing (deleted)
4. `ls netlify/functions/create-payment-intent.ts 2>/dev/null` returns nothing (deleted)
5. `ls netlify/functions/create-checkout.ts 2>/dev/null` returns nothing (deleted)
6. `node -e "console.log(require('./node_modules/stripe/package.json').version)"` starts with `17.`
7. `grep "req.text()" app/api/stripe-webhook/route.ts` returns a match
8. `npx tsc --noEmit` exits 0
9. `npm run build` exits 0
</verification>

<success_criteria>
- stripe@17.x installed and all 14 server files updated to apiVersion '2025-02-24.acacia'
- Zero instances of old apiVersion '2022-11-15' in app/ or lib/
- 3 dead Netlify functions deleted
- Webhook handler confirmed using req.text() (STRIPE-04)
- TypeScript compiles clean; Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-stripe-sdk-upgrade/05-01-SUMMARY.md`
</output>
