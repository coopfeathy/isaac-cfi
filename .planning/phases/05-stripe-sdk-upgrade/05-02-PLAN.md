---
phase: 05-stripe-sdk-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - STRIPE-05

must_haves:
  truths:
    - "@stripe/stripe-js is version 7.x in package.json"
    - "@stripe/react-stripe-js is version 3.x in package.json"
    - "The admin billing page loads Stripe Elements without errors"
    - "A test payment and webhook replay both succeed in Stripe test mode"
  artifacts:
    - path: "package.json"
      provides: "Client Stripe packages at target versions"
      contains: '"@stripe/stripe-js": "^7.9.0"'
  key_links:
    - from: "app/admin/billing/page.tsx"
      to: "@stripe/stripe-js"
      via: "loadStripe import"
      pattern: "loadStripe"
    - from: "app/admin/billing/page.tsx"
      to: "@stripe/react-stripe-js"
      via: "Elements, PaymentElement, useStripe, useElements imports"
      pattern: "Elements|PaymentElement|useStripe|useElements"
---

<objective>
Upgrade the Stripe client packages (@stripe/stripe-js from 1.54.0 to 7.9.0, @stripe/react-stripe-js from 2.1.0 to 3.10.0), verify the full build passes, and run the manual test gate (D-02) with Isaac confirming a test payment and webhook replay both succeed.

Purpose: Complete the Stripe SDK upgrade by bringing client packages in sync with the server SDK. The manual test gate (D-02) is the final validation that the entire Stripe integration works end-to-end after the upgrade.

Output: All Stripe packages at target versions; clean build; human-verified test payment and webhook replay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stripe-sdk-upgrade/05-CONTEXT.md
@.planning/phases/05-stripe-sdk-upgrade/05-RESEARCH.md
@.planning/phases/05-stripe-sdk-upgrade/05-01-SUMMARY.md

<interfaces>
<!-- Client-side Stripe usage in app/admin/billing/page.tsx -->
<!-- The loadStripe() call has NO apiVersion argument (confirmed) — no code change needed -->
<!-- Elements, PaymentElement, useStripe, useElements API surface is unchanged v2 → v3 -->

From app/admin/billing/page.tsx:
```typescript
import { loadStripe } from "@stripe/stripe-js"
import { Elements, PaymentElement, useStripe, useElements } from "@stripe/react-stripe-js"

const stripePromise = loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY || "")
// stripe.confirmPayment() — unchanged API surface
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade @stripe/stripe-js and @stripe/react-stripe-js client packages</name>
  <read_first>
    - package.json
    - app/admin/billing/page.tsx
  </read_first>
  <files>
    package.json
    package-lock.json
  </files>
  <action>
Install both client packages together in a single command (per D-03, D-04):
```bash
npm install @stripe/stripe-js@^7.9.0 @stripe/react-stripe-js@^3.10.0
```

Peer dependency check: `@stripe/react-stripe-js@3.10.0` requires `@stripe/stripe-js >=1.44.1 <8.0.0` — compatible with `^7.9.0`. It also requires `react >=16.8.0 <20.0.0` — the project uses React 18.2.0, which satisfies this.

After install, verify both packages resolved correctly:
```bash
node -e "console.log(require('./node_modules/@stripe/stripe-js/package.json').version)"
node -e "console.log(require('./node_modules/@stripe/react-stripe-js/package.json').version)"
```

Client-side code changes: NONE expected. The `loadStripe()` call in `app/admin/billing/page.tsx` does NOT pass an `apiVersion` argument (this was removed in @stripe/stripe-js v6 and the codebase never used it). `Elements`, `PaymentElement`, `useStripe`, `useElements`, and `stripe.confirmPayment()` have an unchanged API surface from v2 to v3.

Run the full build to verify no TypeScript or runtime issues:
```bash
npm run build
```

Also run lint:
```bash
npm run lint
```
  </action>
  <verify>
    <automated>node -e "const v = require('./node_modules/@stripe/stripe-js/package.json').version; console.log('@stripe/stripe-js:', v); process.exit(v.startsWith('7.') ? 0 : 1)" && node -e "const v = require('./node_modules/@stripe/react-stripe-js/package.json').version; console.log('@stripe/react-stripe-js:', v); process.exit(v.startsWith('3.') ? 0 : 1)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"@stripe/stripe-js": "^7.9.0"` (or a ^7.x range)
    - `package.json` contains `"@stripe/react-stripe-js": "^3.10.0"` (or a ^3.x range)
    - `node_modules/@stripe/stripe-js/package.json` version starts with `7.`
    - `node_modules/@stripe/react-stripe-js/package.json` version starts with `3.`
    - `npm run build` exits with code 0
    - `app/admin/billing/page.tsx` is NOT modified (no code changes needed)
  </acceptance_criteria>
  <done>Both client Stripe packages upgraded to target versions; build passes; no client code changes required</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual test gate — test payment and webhook replay (D-02)</name>
  <files>none</files>
  <action>
This is the D-02 manual test gate. Isaac must perform both tests in Stripe test mode before the phase can be marked complete. No code changes — this is a human verification step.

Present the checklist below to Isaac and wait for confirmation.

**Test 1 — Test payment:**
1. Deploy the branch to a preview environment (or run locally with `npm run dev`)
2. Open the app and navigate to a payment flow (e.g., paying an invoice or the admin billing page)
3. Use Stripe test card `4242 4242 4242 4242` with any future expiry and any CVC
4. Complete the payment
5. Open Stripe Dashboard (test mode) -> Payments
6. Confirm the payment intent was created and succeeded
7. Expected: Payment shows status "Succeeded" with API version `2025-02-24.acacia`

**Test 2 — Webhook replay:**
1. In Stripe Dashboard -> Developers -> Webhooks -> select the `/api/stripe-webhook` endpoint
2. Find the most recent event (from Test 1, or any recent event)
3. Click "Resend" to replay the webhook event
4. Check the webhook response — should show HTTP 200
5. Expected: No signature verification errors, no 400/500 responses

**Both tests must pass before marking this phase complete.**
  </action>
  <verify>Human confirms both tests pass by typing "approved"</verify>
  <done>Isaac confirms test payment succeeded and webhook replay returned HTTP 200 with no errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Stripe.js | Client loads Stripe.js from js.stripe.com CDN; publishable key exposed (by design) |
| Stripe webhook → app | External POST with HMAC signature; verified in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | package.json (client packages) | mitigate | Pin `@stripe/stripe-js@^7.9.0` and `@stripe/react-stripe-js@^3.10.0` (not `*`); commit `package-lock.json` |
| T-05-06 | Information Disclosure | app/admin/billing/page.tsx | accept | Publishable key in `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is designed to be client-visible; no secret key exposure |
| T-05-07 | Spoofing | Stripe.js CDN | accept | Stripe.js loaded from `js.stripe.com` CDN (SRI not supported by Stripe); standard industry practice; risk accepted |
</threat_model>

<verification>
1. `grep "@stripe/stripe-js" package.json` shows `^7.9.0` or compatible 7.x range
2. `grep "@stripe/react-stripe-js" package.json` shows `^3.10.0` or compatible 3.x range
3. `npm run build` exits 0
4. Human confirms: test payment succeeds in Stripe Dashboard (test mode)
5. Human confirms: webhook replay returns HTTP 200 with no signature errors
</verification>

<success_criteria>
- @stripe/stripe-js at 7.x and @stripe/react-stripe-js at 3.x installed
- Full build passes
- Isaac confirms test payment and webhook replay both succeed (D-02 gate)
</success_criteria>

<output>
After completion, create `.planning/phases/05-stripe-sdk-upgrade/05-02-SUMMARY.md`
</output>
