---
phase: 02-admin-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/page.tsx
  - app/admin/tabs/SlotsTab.tsx
  - app/admin/tabs/BookingsTab.tsx
  - app/admin/tabs/ProspectsTab.tsx
  - app/admin/tabs/BlogTab.tsx
  - app/admin/tabs/SocialTab.tsx
  - app/admin/tabs/EmailTab.tsx
  - app/admin/tabs/SettingsTab.tsx
autonomous: true
requirements:
  - ADMIN-08
user_setup: []

must_haves:
  truths:
    - "Each admin tab (Slots, Bookings, Prospects, Blog, Social, Email, Settings) lives in its own component file under app/admin/tabs/"
    - "Admin dashboard loads only the active tab's component via next/dynamic lazy loading"
    - "Switching tabs does not re-fetch data for other tabs — each tab fetches its own data independently on first render"
    - "The admin page.tsx is a thin shell under 200 lines that imports tabs dynamically"
  artifacts:
    - path: "app/admin/tabs/SlotsTab.tsx"
      provides: "Slot management UI with group class appointments"
    - path: "app/admin/tabs/BookingsTab.tsx"
      provides: "Booking review UI"
    - path: "app/admin/tabs/ProspectsTab.tsx"
      provides: "Prospect management UI"
    - path: "app/admin/tabs/BlogTab.tsx"
      provides: "Blog post creation UI"
    - path: "app/admin/tabs/SocialTab.tsx"
      provides: "Social media post management UI"
    - path: "app/admin/tabs/EmailTab.tsx"
      provides: "Email campaigns and support tickets UI"
    - path: "app/admin/tabs/SettingsTab.tsx"
      provides: "Course settings UI (existing) — expanded in Plan 02"
    - path: "app/admin/page.tsx"
      provides: "Thin shell with dynamic imports and tab switching"
  key_links:
    - from: "app/admin/page.tsx"
      to: "app/admin/tabs/*.tsx"
      via: "next/dynamic(() => import('./tabs/XTab'))"
      pattern: "dynamic\\(\\(\\) => import"
---

<objective>
Decompose the 3,275-line `app/admin/page.tsx` monolith into 7 independently lazy-loaded tab components under `app/admin/tabs/`.

Purpose: The monolith loads all state, data fetches, and UI for all 7 tabs on every page load. After decomposition, only the active tab's code and data load, satisfying the "<2 second tab load" success criterion per ADMIN-08.

Output: 7 tab component files + a thin shell page.tsx under 200 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-consolidation/02-CONTEXT.md

<interfaces>
<!-- The monolith currently defines these types at the top of page.tsx. 
     Tab components will need to move their relevant types into their own files. -->

From app/admin/page.tsx:
```typescript
type AdminTab = 'slots' | 'bookings' | 'prospects' | 'blog' | 'social' | 'email' | 'settings'

// The supabase client import pattern used in the monolith:
import { supabase } from '@/lib/supabase'
import type { Slot, Booking } from '@/lib/supabase'
```

From app/admin/layout.tsx:
```typescript
// Layout is server component with AdminTopNav — stays unchanged
import AdminTopNav from "@/app/components/AdminTopNav"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all 7 tab components from the monolith</name>
  <files>
    app/admin/tabs/SlotsTab.tsx,
    app/admin/tabs/BookingsTab.tsx,
    app/admin/tabs/ProspectsTab.tsx,
    app/admin/tabs/BlogTab.tsx,
    app/admin/tabs/SocialTab.tsx,
    app/admin/tabs/EmailTab.tsx,
    app/admin/tabs/SettingsTab.tsx
  </files>
  <read_first>
    app/admin/page.tsx
  </read_first>
  <action>
Extract each tab's rendering block and its associated state/handlers from `app/admin/page.tsx` into standalone `'use client'` components under `app/admin/tabs/`.

Per D-01, create these 7 files: `SlotsTab.tsx`, `BookingsTab.tsx`, `ProspectsTab.tsx`, `BlogTab.tsx`, `SocialTab.tsx`, `EmailTab.tsx`, `SettingsTab.tsx`.

Per D-03, each tab component must fetch its own data independently on first render. Move the relevant `useState` declarations, fetch functions, and event handlers from the monolith into each tab file. Each tab is a self-contained `'use client'` component with its own imports, state, and data fetching.

**Line ranges in the monolith for each tab's JSX block:**
- SlotsTab: lines 1782-2352 (slot management + group class appointments + slot request processing + auto-generate discovery slots)
- BookingsTab: lines 2353-2435 (booking table with AdminBookingPlanner)
- ProspectsTab: lines 2436-2767 (prospect CRUD, card/list views, filtering, convert-to-student)
- BlogTab: lines 2768-2872 (blog post form, draft/publish toggle, image upload)
- SocialTab: lines 2873-2984 (social media post management)
- EmailTab: lines 2985-3125 (email campaigns, support tickets)
- SettingsTab: lines 3126-3246 (course settings only — will be expanded in Plan 02)

**For each tab file:**
1. Add `'use client'` directive at the top
2. Move the tab's type definitions (e.g., `SocialMediaPost`, `SupportTicket`, `ClassAppointment`, `SlotRequest` etc.) into the file that uses them
3. Move relevant `useState` hooks and handler functions for that tab
4. Move the `useEffect` or create a new one that calls the fetch function on mount
5. Import `supabase` from `@/lib/supabase` and any other needed imports (e.g., `Link`, `Image` from next)
6. Import `useAuth` from `@/app/contexts/AuthContext` if the tab needs `user` or `isAdmin`
7. Export default the tab component: `export default function SlotsTab() { ... }`
8. The component renders the same JSX that was inside `{activeTab === 'X' && (...)}` — but without the conditional wrapper

**Important state that belongs to specific tabs:**
- SlotsTab: `slots`, `slotTypeFilter`, `slotStatusFilter`, `slotDateFilter`, `slotMonthFilter`, `slotStartDateFilter`, `slotEndDateFilter`, `showAddSlot`, `editingSlotId`, `newSlot`, `generatingSlots`, `generateSlotsResult`, `slotRequests`, `processingRequestId`, `classAppointments`, `classGroups`, `classInstructors`, `showClassForm`, `editingClassId`, `classSaving`, `classMessage`, `classForm`
- BookingsTab: `bookings` (and `AdminBookingPlanner` import)
- ProspectsTab: `prospects`, `prospectSource`, `prospectView`, `expandedProspectId`, `deletingProspectId`, `creatingProspect`, `creatingAndConvertingProspect`, `prospectFormMessage`, `prospectForm`
- BlogTab: `blogTitle`, `blogAuthor`, `blogExcerpt`, `blogContent`, `blogMessage`, `editingPost` and related blog state
- SocialTab: `socialPosts` and related social state
- EmailTab: `recipientType`, `supportTickets`, `updatingSupportTicketId` and related email/campaign state
- SettingsTab: `courses`, `editingCourseId`, `editingCourseTitle`, `editingCourseDescription`, `savingCourseId`, `courseMessage`

**Shared state** (healthSnapshot, loading, auth): The health dashboard at the top of the admin page is rendered BEFORE any tab content. It stays in page.tsx. The `loading` state and auth check (`useAuth`) also stay in page.tsx. Tabs do NOT need the health snapshot.

Do NOT change any business logic, API calls, or UI appearance. This is a pure extraction — cut from monolith, paste into tab files, adjust imports.
  </action>
  <verify>
    <automated>ls app/admin/tabs/SlotsTab.tsx app/admin/tabs/BookingsTab.tsx app/admin/tabs/ProspectsTab.tsx app/admin/tabs/BlogTab.tsx app/admin/tabs/SocialTab.tsx app/admin/tabs/EmailTab.tsx app/admin/tabs/SettingsTab.tsx && echo "All 7 tab files exist"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist under app/admin/tabs/
    - Each file contains 'use client' as the first line
    - Each file has `export default function` for its component
    - SlotsTab.tsx contains `from('slots')` and `handleGenerateDiscoverySlots`
    - BookingsTab.tsx contains `AdminBookingPlanner`
    - ProspectsTab.tsx contains `from('prospects')` and `handleCreateProspect`
    - BlogTab.tsx contains `blogTitle` and `blogContent`
    - SocialTab.tsx contains `SocialMediaPost` type
    - EmailTab.tsx contains `SupportTicket` type
    - SettingsTab.tsx contains `courses` state and `handleSaveCourse`
  </acceptance_criteria>
  <done>All 7 tab components extracted with their own state, data fetching, and event handlers. No shared state leaks between tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx as a thin shell with dynamic imports</name>
  <files>
    app/admin/page.tsx,
    app/admin/slots/page.tsx,
    app/admin/bookings/page.tsx,
    app/admin/prospects/page.tsx,
    app/admin/blog/page.tsx,
    app/admin/social/page.tsx,
    app/admin/email/page.tsx
  </files>
  <read_first>
    app/admin/page.tsx,
    app/admin/tabs/SlotsTab.tsx,
    app/admin/slots/page.tsx
  </read_first>
  <action>
Per D-02, rewrite `app/admin/page.tsx` to be a thin shell that:

1. Uses `'use client'` directive
2. Imports `useAuth`, `useRouter`, `useSearchParams` from their respective locations
3. Imports `dynamic` from `next/dynamic`
4. Defines lazy-loaded tab components using `next/dynamic`:
   ```typescript
   const SlotsTab = dynamic(() => import('./tabs/SlotsTab'), {
     loading: () => <TabSkeleton />
   })
   const BookingsTab = dynamic(() => import('./tabs/BookingsTab'), {
     loading: () => <TabSkeleton />
   })
   // ... same for all 7 tabs
   ```
5. Defines a simple `TabSkeleton` component inline (a centered spinner or pulsing placeholder)
6. Keeps the `AdminTab` type, `isAdminTab` helper, and the `AdminPageContent` component structure BUT strips all tab-specific state and handlers
7. Keeps the health dashboard section (lines ~1600-1752 in original) that shows on initial load
8. Keeps the tab selector bar (the header section around lines 1757-1778 that shows tab title/description)
9. Replaces each `{activeTab === 'X' && (...huge JSX block...)}` with `{activeTab === 'X' && <XTab />}`
10. Keeps the `Suspense` wrapper exports at the bottom (`AdminPage` and `AdminWorkspacePage`)

The URL stays `/admin?tab=X` per D-02 — no routing structure change.

Also update the existing sub-page wrappers (`app/admin/slots/page.tsx`, `app/admin/bookings/page.tsx`, `app/admin/prospects/page.tsx`, `app/admin/blog/page.tsx`, `app/admin/social/page.tsx`, `app/admin/email/page.tsx`) to continue using `AdminWorkspacePage` — they should still work identically since the shell still exports `AdminWorkspacePage`.

The resulting `page.tsx` should be under 200 lines. It contains: imports, types, TabSkeleton, dynamic imports, AdminPageContent (auth check + health dashboard + tab bar + tab rendering), and the two Suspense wrapper exports.
  </action>
  <verify>
    <automated>wc -l app/admin/page.tsx | awk '{if ($1 < 250) print "PASS: " $1 " lines"; else print "FAIL: " $1 " lines (expected < 250)"}'</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/page.tsx is under 250 lines (target: under 200)
    - app/admin/page.tsx contains `import dynamic from 'next/dynamic'`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/SlotsTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/BookingsTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/ProspectsTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/BlogTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/SocialTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/EmailTab')`
    - app/admin/page.tsx contains `dynamic(() => import('./tabs/SettingsTab')`
    - app/admin/page.tsx exports `AdminWorkspacePage` (existing sub-pages depend on this)
    - `npx next build` completes without errors (or `npx next lint` passes)
    - app/admin/slots/page.tsx still imports from '../page'
  </acceptance_criteria>
  <done>page.tsx is a thin shell under 200 lines. Each tab loads lazily via next/dynamic. All existing admin sub-page routes (/admin/slots, /admin/bookings, etc.) continue to work via AdminWorkspacePage. No visual or functional changes to the admin UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component -> supabase | Tab components use anon-key supabase client with RLS — no change from monolith |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Tab component imports | accept | Dynamic imports load from same-origin bundle; Next.js handles code splitting securely |
| T-02-02 | Information Disclosure | Tab state isolation | accept | Each tab has its own state — no cross-tab state leaks. Same isolation as before (conditional rendering) |
| T-02-03 | Elevation of Privilege | Admin auth check | mitigate | Auth check stays in page.tsx shell (useAuth + isAdmin check). Tabs render only after auth passes. No auth bypass possible since tabs are children of authenticated shell. |
</threat_model>

<verification>
1. `ls app/admin/tabs/*.tsx | wc -l` returns 7
2. `wc -l app/admin/page.tsx` returns under 250
3. `grep -c "dynamic(" app/admin/page.tsx` returns 7
4. `grep "export default function" app/admin/tabs/*.tsx | wc -l` returns 7
5. `npx next build` completes successfully (or at minimum `npx next lint` passes)
</verification>

<success_criteria>
- The 3,275-line monolith is replaced by a thin shell (<200 lines) and 7 tab files
- Each tab component is independently loadable via next/dynamic
- No visual or behavioral changes to the admin UI
- All existing sub-page routes (/admin/slots, /admin/bookings, etc.) continue to work
- Only the active tab's code and data loads on any given page view
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-consolidation/02-01-SUMMARY.md`
</output>
